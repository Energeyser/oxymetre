#include "calculSpO2.h"

void calculSpO2(float periode, oxy myOxy, float* maxACR, float* minACR, float* maxACIR, float* minACIR, absorp myAbsorp){
    //*maxACR = 1995;
    //*maxACR = myAbsorp.ACR;
  // printf("myAbsorp.ACR : %f\n",myAbsorp.ACR );
   // printf("periode : %f\n",periode );
    //printf("maxACR : %f\n",*maxACR );
    /*if(myAbsorp.ACR>*maxACR){
        *maxACR = myAbsorp.ACR;
    }
    if(myAbsorp.ACR<*minACR){
        *minACR = myAbsorp.ACR;
    }
    if(myAbsorp.ACIR>*maxACIR){
        *maxACIR = myAbsorp.ACIR;
    }
    if(myAbsorp.ACIR<*minACIR){
        *minACIR = myAbsorp.ACIR;
    }
    */

}
